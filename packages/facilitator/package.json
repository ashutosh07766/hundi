{
  "name": "@hundi/facilitator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify-ledger": "tsx bin/verify-ledger.ts",
    "serve": "tsx --env-file=../../.env src/serve.ts",
    "smoke:settle": "tsx --env-file=../../.env bin/smoke-settle.ts"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "@hono/zod-validator": "^0.9.0",
    "@hundi/cli": "workspace:*",
    "@hundi/core": "workspace:*",
    "better-sqlite3": "^13.0.3",
    "hono": "^4.13.3",
    "playwright": "^1.62.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "node-gyp": "^11.0.0",
    "typescript": "^5.9.0",
    "tsx": "^4.20.0",
    "vitest": "^4.0.0"
  }
}
